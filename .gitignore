# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
.env
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code
CLAUDE.md
.claude/
.mcp.json

# Codex (mirrors CLAUDE.md — contains private Career-Ops content)
AGENTS.md
.codex/

# CV & Career-Ops (personal — never commit)
CV/

# Claude auto memory (moved here via autoMemoryDirectory — private, never commit)
/memory/

# Private project docs (secrets, war-stories, defect history — never commit)
CREDENTIALS.md
PROJECT-DOSSIER.md
DEFECT-LOG.md
CLAUDE.local.md

# Local scratch (browser profiles, generated PDFs)
tmp/
